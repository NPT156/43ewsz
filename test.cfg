CONTRACT_TYPE
{
    name = FredToSpace 
    title = Launch Fred Kerman to space.
    genericTitle = Launch Fred Kerman to space.
    group = tourism-pack
    description = Launch Fred Kerman above the Karman line! (AKA launch Fred Kerman into space)
    genericDescription = Launch Fred Kerman above the Karman line! (AKA launch Fred Kerman into space)
    sortKey = 1
    synopsis = Launch Fred Kerman into space.
    completedMessage = Fred vows never to go into space again.
    agent = Space Travel Co.
    minExpiry = 500.0
    maxExpiry = 1000.0
    deadline = 500
    cancellable = true
    declinable = true
    prestige = Trivial
    targetBody = Kerbin //Change for different planets.
    maxCompletions = 1
    maxSimultaneous = 1
    rewardScience = 5.0  // Change for different types/hardness
    rewardReputation = 5.0
    rewardFunds = 10000.0
    failureReputation = 3.0
    failureFunds = 1000.0
    advanceFunds = 1000.0  
    

    PARAMETER
    {
        name = ReachSpace
        type = ReachSpace
        targetBody = Kerbin //Rename for different Planets
    }
    REQUIREMENT
    {
    name = ReachSpace
    type = ReachSpace
    }

}
